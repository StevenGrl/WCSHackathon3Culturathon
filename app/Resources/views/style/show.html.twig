{% extends 'base.html.twig' %}

{% block body %}
    <h1>Style</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ style.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ style.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ style.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('style_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('style_edit', { 'id': style.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
