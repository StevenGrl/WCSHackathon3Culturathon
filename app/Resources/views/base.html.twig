<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8"/>
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.13/css/all.css">
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}"/>
    <link rel="stylesheet" href="{{ asset('build/style.css') }}">
    {% block stylesheets %}
    {% endblock %}
</head>
<body>
<nav id="navbar" class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div id="drop_one" class="nav-item navbar-brand">
        <a class="nav-link text-light" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
           aria-haspopup="true" aria-expanded="false">
            Menu de l'oeuvre
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <a class="dropdown-item" href="#">Artiste</a>
            <a class="dropdown-item" href="#">Style</a>
            <a class="dropdown-item" href="#">Epoque</a>
        </div>
    </div>
    <div class="ml-5">
        <a class="navbar-brand nav-item text-light"
           href="#">
            {% if artWork is defined %}
                {{ artWork.date|date('Y') }} | ¨{{ artWork.name }}¨ de {{ artWork.artist }}
            {% else %}
                CULTURATHON
            {% endif %}
        </a>
    </div>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <div class="dropdown-divider"></div>
        <ul class="navbar-nav ml-auto">
            <li id="drop_two" class="nav-item navbar-brand">
                <a class="nav-link text-light" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                   aria-haspopup="true" aria-expanded="false">
                    Mes favoris
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="#">favoris 1</a>
                    <a class="dropdown-item" href="#">favoris 2</a>
                    <a class="dropdown-item" href="#">favoris 3</a>
                </div>
            </li>
            <li class="nav-item align-self-center d-lg-none">
                <a class="nav-link" href="#">Fonction secrète</a>
            </li>
            <li class="nav-item align-self-center">
                <a class="nav-link" href="{{ path('fos_user_security_login') }}">login</a>
            </li>
            <li class="nav-item align-self-center">
                <a class="nav-link" href="/">acceuil</a>
            </li>
        </ul>
    </div>
</nav>
{% block free %}
    <div class="container">
        {% block body %}
        {% endblock %}
    </div>
{% endblock %}
<script src="{{ asset('build/app.js') }}"></script>
{% block javascripts %}{% endblock %}
</body>
</html>
