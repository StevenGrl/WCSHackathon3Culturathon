{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tech</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tech.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tech.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tech_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tech_edit', { 'id': tech.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
